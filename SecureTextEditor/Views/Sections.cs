namespace SecureTextEditor.Views
{
    /// <summary>
    /// Contains all available Sections in SaveDialog.
    /// </summary>
    public enum Sections
    {
        Encryption,
        PBE,
        Integrity
    }
}